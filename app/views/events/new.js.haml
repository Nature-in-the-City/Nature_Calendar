- if @suggested_event
    $('#panel').html("#{j render 'suggest_panel'}");
- else
    $('#panel').html("#{j render 'new_panel'}");
setup_event_form('#create');
$("#cancel").on('click', function() {
$('#panel').html("#{j render 'default_panel'}");
});